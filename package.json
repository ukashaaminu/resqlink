{
  "name": "resq-link",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "mobile",
    "server"
  ],
  "scripts": {
    "lint": "echo \"lint placeholder - configure ESLint in each workspace\"",
    "test": "echo \"tests not configured\"",
    "dev:server": "cd server && npm run dev",
    "dev:mobile": "cd mobile && npm start"
  }
}
